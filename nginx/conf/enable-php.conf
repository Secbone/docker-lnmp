        location ~ [^/]\.php(/|$)
        {
            #try_files $uri =404;
            fastcgi_pass  unix:/socks/php-cgi.sock;
            fastcgi_index index.php;
            include conf/fastcgi.conf;
	          include conf/pathinfo.conf;
        }
